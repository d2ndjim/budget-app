<nav class="bg-[#3778C2] text-white p-2">
  <ul class="flex justify-between mr-6">
    <li class="text-4xl">
      <i class="fa fa-navicon"></i>
    </li>
    <li>
      <h2 class="font-bold text-3xl">Categories</h2>
    </li>
    <li class="font-bold text-4xl">
      <%= button_to destroy_user_session_path, method: :delete do %>   
        <i class="fa fa-sign-out"></i>
      <% end %>
    </li>
  </ul>
</nav>
<%= render 'layouts/alerts' %>
<div id="categories">
  <% if @categories.any? %>
    <% @categories.each do |category| %>
      <%= render category %>
    <% end %>
  <% else %>
  <p class="text-base text-center font-bold">There are no categories. Please add a category</p>
  <% end %>
</div>
<div class="bg-[#5FB523] p-2 flex justify-center fixed-bottom">
  <%= link_to "New category", new_category_path, class: "btn btn-outline-light w-75 p-2" %>
<div>
